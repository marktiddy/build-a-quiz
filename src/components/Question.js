import React from "react";
import { Form } from "react-bootstrap";

const Question = ({ question, sendAnswer }) => {
  console.log(question);
  return (
    <>
      <h1>Question Component</h1>
    </>
  );
};

export default Question;
